# v0.17.0 — Gemini (Antigravity) 어댑터 (cost-unknown)  (2026-07-09)

## 한 줄 요약
Google Antigravity(Gemini IDE)가 소스로 — 시간·세션만(가장 얇은 cost-unknown 소스).

## 데이터 실태 (그리고 gemini-cli)

- **진짜 `gemini-cli`(google-gemini/gemini-cli)의 대화·토큰 로그 = 이 머신에 없음**(하드월).
- 있는 건 **Antigravity(Gemini 기반 IDE)** 상태물:
  `~/.gemini/antigravity/brain/<uuid>/*.metadata.json` = {artifactType, summary, `updatedAt`(ISO)}.
- **토큰·모델·프롬프트 없음, updatedAt 시각만** → providesCost=false. 지금까지 중 가장 얇은 신호.

## 무엇이 바뀌었나

- **`adapters/gemini.ts`(신규):** `brain/<uuid>` 마다 세션 1개, `*.metadata.json`의 `updatedAt`으로
  시간창. 토큰 0, 모델 `gemini/antigravity`. 내용 다이제스트 없음(프롬프트 미저장).
- **`ANALYSIS_ADAPTERS` 등록:** claude · cursor · codex · copilot · **gemini-antigravity**.
- **caveat 일반화:** "세션 정의는 도구마다 다릅니다(Claude=세션 로그 · Cursor/Copilot=대화 ·
  Codex=rollout · Gemini=작업). 비용 미상 소스는 시간·빈도만."

## 정직성

- Antigravity는 **시간·세션 수만** — 비용·모델·무엇을했나 전부 못 함(프롬프트·토큰 부재). "도구별 사용"에만 등장.
- gemini-cli 진짜 로그가 생기면 별도 어댑터로(이 어댑터는 Antigravity 전용).

## 테스트

`test/gemini.test.ts`(3): uuid별 세션·updatedAt 시간창·토큰0·providesCost=false·부재 조용. 268 그린.
실측: `analyze` "도구별 사용"에 `Gemini (Antigravity): 세션 13 · 비용 미상`.

## 소스 현황 (5개)
Claude Code(비용O) · Codex CLI(비용O) · Cursor(시간) · GitHub Copilot(시간·요청) · Gemini/Antigravity(시간).
비용 비교 = Claude·Codex만. 나머지 = 시간·활동 가시성.
