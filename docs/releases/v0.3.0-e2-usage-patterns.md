# v0.3.0 — E2 사용 패턴 발견 엔진

릴리스: 2026-06-15 (KST)

## 한 줄 요약
초상의 `## 발견`이 세션 데이터만으로 "어떻게 쓰는지" 패턴(케이던스·요일·추세·비용급증)을 보여준다.

## 산출물 before/after
**before** — `## 발견`은 E1 미니 인사이트 2개(비용 집중·주력 모델)뿐.

**after** — 사용 패턴 4축이 합성되어(상한 5), 이 레포 실데이터 예:
```
## 발견
- 평균 세션 약 13.9h, 기간 36일 중 11일 활동입니다.
- 가장 활발한 요일은 일요일입니다.
- 후반부 사용이 전반부의 약 6.7배 — 최근 더 자주 씁니다.
- 비용이 튄 날: 2026-05-17, 2026-05-31 (평소 중앙값 $267.06의 최대 약 2.8배).
- 토큰의 100%를 Opus로 썼습니다 (단일 모델 집중).
> 결정적 관찰(활동일 11일 기준). 추세·상관 서술이지 통계적 단정·인과가 아닙니다.
```
- 전부 결정적(git·LLM 불요), 작은 n 가드로 거짓 패턴 침묵, "활동일 N일 기준" 정직성 라벨.
- cost-spike가 뜨면 E1 cost-concentration은 빠진다(비용 이중표기 방지).

## 검증
- 테스트: **109 → 123 그린**. 신규 `patterns`(8)·`findings`(3)·`day`(2) + portrait 확장(1).
- `npx tsc --noEmit` / `npm run build` 클린.

## AI 사용 메타 — 도그푸딩
AIMM 자신의 `analyze`로 측정(2026-06-15):
- **AI-Metrics-MCP 프로젝트: 세션 3 · 추정 비용 약 $843.80** (개발 기간 2026-06-10 ~ 06-15).

> ⚠️ 기간 기반 근사다. 같은 기간 전체 Claude Code 사용이며, '시간'은 세션 지속 추정이지
> 근무시간이 아니다. 비용은 추정치(단가 v2026-06-10, 정산액 아님). 토큰·세션은 양이지 실력이 아니다.

## 런타임 벤치
N/A: 핫패스 아님(순수 집계, 전체 테스트 123개 ~0.5초).

## 본인 메모
_(선택 — 비워둠)_

## 다음
git 생산성 상관(디버깅↔fix 등)은 "설득 문서" 단계로 후속. analyze 통합·LLM 산문화도 후속.
